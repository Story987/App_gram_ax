name: 📝 Markdown Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # =============== ОСНОВНАЯ ДЖОБА: ПРОВЕРКА И КОНВЕРТАЦИЯ ===============
  markdown-processing:
    name: 🔍 Проверка и конвертация .md файлов
    runs-on: ubuntu-latest
    
    steps:
      # 1. Загружаем код (используем последнюю стабильную версию)
      - name: 📥 Checkout
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0

      # 2. Устанавливаем Node.js (используем LTS версию без deprecated)
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4.3.0
        with:
          node-version: '22'

      # 3. Устанавливаем утилиты для работы с Markdown
      - name: 📦 Install markdown tools
        run: |
          npm install -g markdownlint-cli
          npm install -g marked
          npm install -g prettier

      # 4. Проверка стиля Markdown (линтер) - не падаем при ошибках
      - name: 🔍 Lint Markdown files
        continue-on-error: true
        run: |
          echo "🔍 Проверяем все .md файлы..."
          find . -name "*.md" -not -path "./node_modules/*" -not -path "./.git/*" -print0 | xargs -0 markdownlint || true
          echo "✅ Проверка завершена"

      # 5. Форматирование Markdown (авто-исправление)
      - name: 💅 Format Markdown files
        continue-on-error: true
        run: |
          echo "💅 Форматируем .md файлы..."
          find . -name "*.md" -not -path "./node_modules/*" -not -path "./.git/*" -print0 | xargs -0 prettier --write --prose-wrap=always || true
          echo "✅ Форматирование завершено"

      # 6. Конвертация .md в .html
      - name: 🌐 Convert MD to HTML
        run: |
          echo "🌐 Конвертируем .md в .html..."
          mkdir -p html_output
          # Находим все .md файлы и конвертируем их
          find . -name "*.md" -not -path "./node_modules/*" -not -path "./.git/*" -print0 | while IFS= read -r -d '' file; do
            # Определяем имя файла без расширения
            filename=$(basename "$file" .md)
            # Определяем путь относительно корня
            relpath=$(realpath --relative-to="." "$file" | sed 's/\.md$//')
            # Создаём структуру папок
            mkdir -p "html_output/$(dirname "$relpath")"
            # Конвертируем
            marked "$file" > "html_output/${relpath}.html"
            echo "  ✅ ${file} → html_output/${relpath}.html"
          done
          echo "✅ Конвертация завершена"

      # 7. Генерация индекса (список всех .md файлов)
      - name: 📋 Generate Markdown index
        run: |
          echo "📋 Генерируем индекс всех .md файлов..."
          echo "# 📚 Список Markdown файлов в репозитории" > INDEX.md
          echo "" >> INDEX.md
          echo "Всего файлов: $(find . -name "*.md" -not -path "./node_modules/*" -not -path "./.git/*" | wc -l)" >> INDEX.md
          echo "" >> INDEX.md
          echo "| Файл | Путь |" >> INDEX.md
          echo "|------|------|" >> INDEX.md
          find . -name "*.md" -not -path "./node_modules/*" -not -path "./.git/*" | sort | while read file; do
            # Убираем ./ в начале
            clean_file="${file#./}"
            echo "| [${clean_file}](${clean_file}) | \`${clean_file}\` |" >> INDEX.md
          done
          echo "✅ Индекс создан: INDEX.md"

      # 8. Сохраняем результаты как артефакт
      - name: 📦 Upload artifacts
        uses: actions/upload-artifact@v4.4.3
        with:
          name: markdown-output
          path: |
            *.md
            html_output/
            INDEX.md
          retention-days: 30

  # =============== ДЖОБА: ПРОВЕРКА ССЫЛОК (ОПЦИОНАЛЬНО) ===============
  check-links:
    name: 🔗 Проверка битых ссылок
    runs-on: ubuntu-latest
    needs: markdown-processing
    # Пропускаем, если предыдущая джоба была отменена или не запускалась
    if: always() && needs.markdown-processing.result != 'cancelled'
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4.2.2

      - name: 🔗 Check broken links
        uses: gaurav-nelson/github-action-markdown-link-check@v1
        with:
          use-quiet-mode: 'yes'
          use-verbose-mode: 'yes'
          folder-path: '.'
          file-path: '*.md'
          max-depth: 2
        continue-on-error: true  # Не падаем, если есть битые ссылки
